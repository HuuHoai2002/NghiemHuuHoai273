@model NghiemHuuHoai273.Models.CompanyNHH273

@{
  ViewData["Title"] = "Edit";
}

<h4 class="py-5">Sửa thông tin Company</h4>

<div class="row">
  <div class="col-md-4">
    <form method="post" asp-action="Edit">
      <span asp-validation-sumary="ModelOnly"></span>
      <input type="hidden" value="@Model.CompanyId" asp-for="CompanyId" />
      <div class="form-group">
        <label asp-for="CompanyName" class="control-label">Name</label>
        <input asp-for="CompanyName" class="control-label" value="@Model.CompanyName" />
        <span asp-validation-for="CompanyName" class="text-danger"></span>
      </div>
      <div class="form-group">
        <input type="submit" value="Cập nhật" class="btn btn-primary" /> |
        <a class="btn btn-success" asp-action="Index">Danh sách Person</a>
      </div>
    </form>
  </div>
</div>

@section Scripts {
@{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}